# 团队网格搜索任务清单

维护者汇总所有 `results/grid_search/` 下的结果后，选定最优超参并**自行跑全量**（`01_full_train.ipynb`，8 epoch，全量用户评估）。  
团队成员**仅完成下表 FAST 快筛任务**。

完整说明见 [results/grid_search/README.md](results/grid_search/README.md)。

---

## 统一配置（所有任务必须一致）

在 [`notebooks/02_grid_search.ipynb`](notebooks/02_grid_search.ipynb) 中保持：

```python
FAST_MODE = True
fast_num_epochs = 3
fast_eval_user_limit = 100000
eval_k = 10
seed = 42
num_workers = 0
weight_decay = 0.0
# grid_space 内 eval_num_neg 为 [100]
```

**禁止**：`FAST_MODE = False`（全量复核仅维护者执行）。

---

## 任务表

### 任务 A（必做，1 组）

| ID | 分配人 | 状态 |
|----|--------|------|
| A1 | （填写） | 待跑 |

**超参**：maxlen=160, hidden_units=128, num_blocks=4, num_heads=2, dropout_rate=0.15

```python
grid_space = {
    "maxlen": [160],
    "hidden_units": [128],
    "num_blocks": [4],
    "num_heads": [2],
    "dropout_rate": [0.15],
    "lr": [1e-3],
    "batch_size": [512],  # CPU 改为 256
    "eval_num_neg": [100],
}
```

---

### 任务 B（建议，8 组）— 可拆给 2 人各 4 组

固定 **maxlen=100, hidden_units=128**，补全 blocks × heads × dropout 组合。

| ID | num_blocks | num_heads | dropout_rate | 分配人 | 状态 |
|----|------------|-----------|--------------|--------|------|
| B1 | 2 | 2 | 0.15 | | 待跑 |
| B2 | 2 | 2 | 0.20 | | 待跑 |
| B3 | 2 | 4 | 0.15 | | 待跑 |
| B4 | 2 | 4 | 0.20 | | 待跑 |
| B5 | 3 | 2 | 0.15 | | 待跑 |
| B6 | 3 | 2 | 0.20 | | 待跑 |
| B7 | 3 | 4 | 0.15 | | 待跑 |
| B8 | 3 | 4 | 0.20 | | 待跑 |

单组任务示例（以 B1 为例，其余改对应三列）：

```python
grid_space = {
    "maxlen": [100],
    "hidden_units": [128],
    "num_blocks": [2],
    "num_heads": [2],
    "dropout_rate": [0.15],
    "lr": [1e-3],
    "batch_size": [512],
    "eval_num_neg": [100],
}
```

若一人认领多组，可合并为列表，例如 B1+B2：

```python
grid_space = {
    "maxlen": [100],
    "hidden_units": [128],
    "num_blocks": [2],
    "num_heads": [2],
    "dropout_rate": [0.15, 0.2],
    "lr": [1e-3],
    "batch_size": [512],
    "eval_num_neg": [100],
}
```

---

### 任务 C（可选，1 组）

| ID | 分配人 | 状态 |
|----|--------|------|
| C1 | | 待跑 |

**超参**：maxlen=100, hidden_units=64, num_blocks=3, num_heads=4, dropout_rate=0.2

```python
grid_space = {
    "maxlen": [100],
    "hidden_units": [64],
    "num_blocks": [3],
    "num_heads": [4],
    "dropout_rate": [0.2],
    "lr": [1e-3],
    "batch_size": [512],
    "eval_num_neg": [100],
}
```

---

## 操作步骤

1. Clone 仓库并准备 `data/` 下 4 个 parquet（见 [使用指南.md](使用指南.md)）。
2. 打开 `notebooks/02_grid_search.ipynb`，运行路径 cell 与 memmap 准备 cell。
3. 将「阶段配置」cell 中的 `grid_space` 换成你认领任务的配置。
4. 运行 `build_experiments` 与实验循环 cell。
5. 将生成的 csv/jsonl **重命名**为 `grid_search_fast_{姓名}_{任务ID}_{时间}.csv` 放入 `results/grid_search/`。
6. 提 PR 或发给维护者。

---

## 已有历史结果（供对照）

维护者已跑 fast 结果见 `results/grid_search/history/`。当前 fast 最优约：

- maxlen=160, hidden=128, num_blocks=3, dropout=0.15  
- valid_ndcg10 ≈ 0.897（10 万用户评估）

任务 A1（128×blocks4@160）用于验证是否能超过上述结果。

---

## 维护者（你）后续

```bash
python scripts/merge_grid_results.py
```

按 `valid_ndcg10` 选 Top1 → 修改 `01_full_train.ipynb` 超参 → 全量训练并更新 baseline。
